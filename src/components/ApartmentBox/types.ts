//import * as dayjs from "dayjs";
//import { DateRange } from "@mui/lab";

export interface ApartmentBoxProps {
  icon: string;
  type: string;
  // dateRange: DateRange<dayjs.Dayjs>;
  // dateRange: DateRange<Dayjs>;
  dateRange: string;
  availability: number;
}
